{
  "name": "bilibili-accelerator-client",
  "version": "0.1.3",
  "private": true,
  "description": "Inject Bilibili Accelerator into the Bilibili Windows desktop client player.",
  "license": "MIT",
  "scripts": {
    "check": "node --check src/launcher.cjs",
    "build:win": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build-win.ps1"
  },
  "devDependencies": {
    "postject": "1.0.0-alpha.6"
  }
}
